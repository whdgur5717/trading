# 프론트엔드 엔드포인트 실패 처리

프론트엔드 기능 화면은 백엔드 API 오류 본문이 있는 엔드포인트 실패만 직접
처리한다. 생성된 API 호출은 엔드포인트 실패를 `Failure`로 유지하고,
query-options 계층에서 React Query 오류로 바꾼다. 네트워크 실패, 타임아웃,
스키마 오류, 예상하지 못한 상태 코드, 런타임 오류는 화면별로 다시 구현하지 않고
공통 오류 처리로 보낸다.
